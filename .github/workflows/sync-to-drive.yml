- name: Sync to Google Drive
        run: |
          rclone sync . gdrive: \
            --exclude ".git/**" \
            --exclude ".github/**" \
            --drive-service-account-file ~/.config/rclone/sa.json \
            --drive-impersonate tua-email-google@gmail.com \
            -v
